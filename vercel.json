{
  "buildCommand": "cp -r ../shared ./shared && pwd && ls -la && cat package.json && npm install --legacy-peer-deps && npm run build",
  "outputDirectory": "packages/frontend/dist",
  "framework": "vite",
  "installCommand": "echo 'Skipping root install'",
  "devCommand": "npm run dev"
}
