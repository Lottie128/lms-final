[phases.setup]
nixPkgs = ['...', 'curl', 'unzip']

[phases.install]
cmds = [
  'curl -fsSL https://bun.sh/install | bash',
  '~/.bun/bin/bun install'
]

[phases.build]
cmds = [
  'cd packages/backend && npm install tsx',
  'cd packages/backend && ~/.bun/bin/bunx prisma generate'
]

[start]
cmd = 'cd packages/backend && npm start'
